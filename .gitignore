# Created by https://www.toptal.com/developers/gitignore/api/python
# Edit at https://www.toptal.com/developers/gitignore?templates=python

### Python ###
# Byte-compiled / optimized / DLL files
src/api/__pycache__/
src/api/*.py[cod]
src/api/*$py.class

# C extensions
src/api/*.so

# Distribution / packaging
src/api/.Python
src/api/build/
src/api/develop-eggs/
src/api/dist/
src/api/downloads/
src/api/eggs/
src/api/.eggs/
src/api/lib/
src/api/lib64/
src/api/parts/
src/api/sdist/
src/api/var/
src/api/wheels/
src/api/share/python-wheels/
src/api/*.egg-info/
src/api/.installed.cfg
src/api/*.egg
src/api/MANIFEST

# PyInstaller
#  Usually these files are written by a python script from a template
#  before PyInstaller builds the exe, so as to inject date/other infos into it.
src/api/*.manifest
src/api/*.spec

# Installer logs
src/api/pip-log.txt
src/api/pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/
cover/

# Translations
src/api/*.mo
src/api/*.pot

# Django stuff:
src/api/*.log
src/api/local_settings.py
src/api/db.sqlite3
src/api/db.sqlite3-journal

# Sphinx documentation
src/api/docs/_build/

# PyBuilder
src/api/.pybuilder/
src/api/target/

# IPython
src/api/profile_default/
src/api/ipython_config.py

# pyenv
#   For a library or package, you might want to ignore these files since the code is
#   intended to run in multiple environments; otherwise, check them in:
# .python-version

# pipenv
#   According to pypa/pipenv#598, it is recommended to include Pipfile.lock in version control.
#   However, in case of collaboration, if having platform-specific dependencies or dependencies
#   having no cross-platform support, pipenv may install dependencies that don't work, or not
#   install all needed dependencies.
#Pipfile.lock

# poetry
#   Similar to Pipfile.lock, it is generally recommended to include poetry.lock in version control.
#   This is especially recommended for binary packages to ensure reproducibility, and is more
#   commonly ignored for libraries.
#   https://python-poetry.org/docs/basic-usage/#commit-your-poetrylock-file-to-version-control
#poetry.lock

# pdm
#   Similar to Pipfile.lock, it is generally recommended to include pdm.lock in version control.
#pdm.lock
#   pdm stores project-wide configurations in .pdm.toml, but it is recommended to not include it
#   in version control.
#   https://pdm.fming.dev/#use-with-ide
src/api/.pdm.toml

# PEP 582; used by e.g. github.com/David-OConnor/pyflow and github.com/pdm-project/pdm
src/api/__pypackages__/

# Environments
src/api/.env
src/api/.venv
src/api/env/
src/api/venv/
src/api/ENV/
src/api/env.bak/
src/api/venv.bak/

# Spyder project settings
src/api/.spyderproject
src/api/.spyproject

# Rope project settings
src/api/.ropeproject

# mkdocs documentation
src/api//site

# mypy
src/api/.mypy_cache/
src/api/.dmypy.json
src/api/dmypy.json

# Pyre type checker
src/api/.pyre/

# pytype static type analyzer
src/api/.pytype/

# Cython debug symbols
src/api/cython_debug/

# PyCharm
#  JetBrains specific template is maintained in a separate JetBrains.gitignore that can
#  be found at https://github.com/github/gitignore/blob/main/Global/JetBrains.gitignore
#  and can be added to the global gitignore or merged into this file.  For a more nuclear
#  option (not recommended) you can uncomment the following to ignore the entire idea folder.
#.idea/

### Python Patch ###
# Poetry local configuration file - https://python-poetry.org/docs/configuration/#local-configuration
src/api/poetry.toml

# ruff
src/api/.ruff_cache/

# LSP config files
src/api/pyrightconfig.json

# End of https://www.toptal.com/developers/gitignore/api/python

# Allure reports
src/api/*my_allure/

*.vscode/
**.DS_Store

# Miscellaneous
src/api/*.class
src/api/*.log
src/api/*.pyc
src/api/*.swp
src/api/.DS_Store
src/api/.atom/
src/api/.buildlog/
src/api/.history
src/api/.svn/
src/api/migrate_working_dir/

# IntelliJ related
src/api/*.iml
src/api/*.ipr
src/api/*.iws
.idea/

# Flutter/Dart/Pub related
src/app/**/doc/api/
src/app/**/ios/Flutter/.last_build_id
src/app/.dart_tool/
src/app/.flutter-plugins
src/app/.flutter-plugins-dependencies
src/app/.packages
src/app/.pub-cache/
src/app/.pub/
src/app/build/

# Symbolication related
src/app/app.*.symbols

# Obfuscation related
src/app/app.*.map.json

# Android Studio will place build artifacts here
src/app/android/app/debug
src/app/android/app/profile
src/app/android/app/release
